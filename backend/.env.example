# ==========================================
# Backend Environment Variables
# ==========================================

# ==========================================
# MongoDB Connection (⚠️ SENSITIVE!)
# ==========================================
# Local MongoDB:
MONGODB=mongodb://localhost:27017/studyroom

# MongoDB Atlas (for production - recommended):
# MONGODB=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Alternative environment variable names (the app checks both):
# MONGO=mongodb://localhost:27017/studyroom


# ==========================================
# Authentication & Security
# ==========================================
# JWT Secret - MUST be a strong random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-123456789


# ==========================================
# Push Notifications (Browser Push)
# ==========================================
# Generate VAPID keys with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here
VAPID_SUBJECT=mailto:your-email@example.com


# ==========================================
# Server Configuration
# ==========================================
PORT=5000


# ==========================================
# Google OAuth (Optional - for Google Sign-In)
# ==========================================
# Get these from: https://console.cloud.google.com/
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback


# ==========================================
# Instructions:
# ==========================================
# 1. Copy this file to .env in backend/server/ directory
# 2. Replace ALL placeholder values with your actual credentials
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. For production deployment:
#    - Use MongoDB Atlas URL
#    - Generate a strong JWT_SECRET
#    - Generate VAPID keys for push notifications
# 5. Add these to your hosting platform's environment variables
#    (Render.com, Heroku, etc.)
